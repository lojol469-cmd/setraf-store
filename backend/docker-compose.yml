version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: center-store-backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - MONGO_URI=mongodb+srv://SETRAF:Dieu19961991%3F%3F%21%3F%3F%21@cluster0.5tjz9v0.mongodb.net/myDatabase14?retryWrites=true&w=majority&appName=Cluster0
      - MONGO_DB_NAME=myDatabase14
      - JWT_SECRET=Dieu19961991??!??!
      - JWT_REFRESH_SECRET=Dieu19961991??!??!_refresh
      - CLOUDINARY_CLOUD_NAME=dddkmikpf
      - CLOUDINARY_API_KEY=848997274359864
      - CLOUDINARY_API_SECRET=5TZZdDTf7gmAFk_eVpJWeKgilb8
      - FRONTEND_URL=https://your-frontend-domain.com
      - EMAIL_USER=nyundumathryme@gmail.com
      - EMAIL_PASS=zsrrymlixizhiybl
    volumes:
      - uploads:/app/uploads
    networks:
      - center-store-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on:
      - mongo

  mongo:
    image: mongo:6.0
    container_name: center-store-mongo
    restart: unless-stopped
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=Dieu19961991??!??!
      - MONGO_INITDB_DATABASE=myDatabase14
    volumes:
      - mongo_data:/data/db
      - ./mongo-init:/docker-entrypoint-initdb.d
    ports:
      - "27017:27017"
    networks:
      - center-store-network
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  mongo_data:
  uploads:

networks:
  center-store-network:
    driver: bridge